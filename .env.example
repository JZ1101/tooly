# =============================================================================
# SpoonOS Agent Configuration Example
# =============================================================================
# Copy this file to .env and fill in your actual API keys and configuration
# DO NOT commit .env with real credentials to version control
# =============================================================================

# =============================================================================
# LLM Provider API Keys (set at least one)
# =============================================================================
GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
DEEPSEEK_API_KEY=your_deepseek_key_here
OPENROUTER_API_KEY=your_openrouter_key_here

# =============================================================================
# Default LLM Settings (Optional)
# =============================================================================
DEFAULT_LLM_PROVIDER=openai           # Options: openai, gemini, anthropic, deepseek, openrouter
DEFAULT_MODEL=gpt-4o-mini             # Model name for the selected provider
# GEMINI_MAX_TOKENS=20000             # Context limit for Gemini (uncomment if using Gemini)

# =============================================================================
# Web3 Configuration (Required for EVM tools)
# =============================================================================
WEB3_PROVIDER_URL=https://mainnet.infura.io/v3/your_project_id
PRIVATE_KEY=your_private_key_here
RPC_URL=https://mainnet.infura.io/v3/your_project_id

# =============================================================================
# Bitquery Configuration (Required for crypto data tools)
# =============================================================================
BITQUERY_CLIENT_ID=your_bitquery_client_id
BITQUERY_CLIENT_SECRET=your_bitquery_client_secret

# =============================================================================
# OKX Web3 Configuration (Required for PowerData tools)
# =============================================================================
OKX_API_KEY=your_okx_api_key
OKX_SECRET_KEY=your_okx_secret_key
OKX_API_PASSPHRASE=your_okx_passphrase
OKX_PROJECT_ID=your_project_id
OKX_BASE_URL=https://web3.okx.com/api/v5/

# =============================================================================
# Chainbase Configuration (Required for Chainbase tools)
# =============================================================================
CHAINBASE_API_KEY=your_chainbase_api_key
CHAINBASE_HOST=0.0.0.0
CHAINBASE_PORT=8000
CHAINBASE_PATH=/sse

# =============================================================================
# Solana Configuration (Required for Solana tools)
# =============================================================================
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# =============================================================================
# Neo Blockchain Configuration (Required for Neo tools)
# =============================================================================
NEO_MAINNET_RPC=https://mainmagnet.ngd.network:443
NEO_TESTNET_RPC=https://testmagnet.ngd.network:443
NEO_RPC_TIMEOUT=20                    # Timeout in seconds (default: 60)
NEO_RPC_ALLOW_INSECURE=false          # Set true to skip TLS verification

# =============================================================================
# GitHub Configuration (Required for GitHub tools)
# =============================================================================
GITHUB_TOKEN=your_github_personal_access_token

# =============================================================================
# Notes:
# =============================================================================
# 1. At minimum, configure one LLM provider (OPENAI_API_KEY recommended)
# 2. Only configure API keys for tools you plan to use
# 3. For blockchain operations, ensure you have sufficient funds in your wallet
# 4. GitHub token needs 'repo' scope for full functionality
# 5. Keep your .env file secure and never commit it to version control
# =============================================================================